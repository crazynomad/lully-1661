// Careers (`/trabalha-connosco`) feature — public API.
// Reads openPosition collection; application form posts via server action;
// CV uploads route through Vercel Blob with 30-day expiry per architecture.md § 8.3.
//
// Phase 2d stub.
export {};
